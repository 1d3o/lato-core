General:

- Add task to copy configs file on initializer generator command.
- Remove old tasks.
- Create generator to generate all files needed for a simple crud.
- Update sort of menu items to use title when position has same value.
- Update some style configurations.

Cells:

- Update widget index to show humanized search key on search input.

Interface:

- Update 'core__get_application_gems' function to return an hash of gems with the version number.

Widgets:

- Update systeminfo widget to show the lato module version.

Languages:

- Add IT language.
- Fix form -> password_help text.