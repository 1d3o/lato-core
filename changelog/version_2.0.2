Interface:

- Fix cell widget function error with mysql when tring to exec a query without a search param.

Cells:

- Add id option on flash cell.
- Add search key on widget index cell.

General:

- Fix margin on my profile menu list.
- Explicit helpers import on controllers to fix production issues.
- Update load configs and languages tasks.
- Add tasks to load configs and languages on the same time.
- Initialize generator to generate minimal configurations to start to work with lato.